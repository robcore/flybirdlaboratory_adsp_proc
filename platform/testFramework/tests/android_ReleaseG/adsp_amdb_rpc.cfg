# Please provide test so paths in the following format
#
#     file://<path_to_foo.so>?<bar>
#
# path_to_foo.so:   This URI path (upto the ?) provides the path to the so
# bar :              The query portion (following the ?) provides the name of the function to call
#
# For more information, take a look at:
#
#     readme.txt

file://./adsp_amdb_rpc.so?adsp_amdb_rpc_test
